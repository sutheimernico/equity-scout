.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
*.db
# SQLite WAL sidecars: they carry committed page data, so they are DB content too.
*.db-wal
*.db-shm
*.log
data/cache/
data/prices/
data/logos/
.state/
.env
.claude/settings.local.json
frontend/node_modules/
frontend/dist/
keepalive.log
.env.bak-*

# Minute-bar store for the signal matrix: ~70 tickers x 11 years, several GB. Reproducible
# via scripts/fetch_minute_history.py, so it is derived data, not source.
data/minutes/

# News history store (Alpaca/Benzinga wire), derived data — see scripts/run_news_latency.py
data/news/
data/matrix_cells.jsonl
